# NANOSUDO.COM — бэклог задач

> Статусы: `[ ]` todo · `[~]` in progress · `[x]` done · `[!]` blocked — ждёт действия от вас
> Обновлено: 2026-08-16

## Статус по исходному запросу (для быстрой сверки)

| # | Задача | Статус |
|---|---|---|
| 1 | Hero — убрать фото/имя, пересобрать по best practice | ✅ готово (Phase 1) |
| 2 | Единый фон/свечение по секциям главной | ✅ готово (Phase 2) |
| 3 | Telegram-уведомления по заявкам | ✅ готово и проверено вживую на проде (Phase 3) |
| 4 | Кейсы: убрать kazpravda/amiray-kz, скрин на главной, обновить nativetravel | ✅ готово (Phase 4) |
| 5 | SEO под Казахстан | 🟡 техника + GSC + Яндекс.Вебмастер готовы; реальные данные показали, что нужен контент (Phase 7) |
| 6 | Почта: `sales@nanosudo.com` → личная | ✅ готово — было настроено ещё до старта работы (Cloudflare Email Routing → `roorsayan@gmail.com`), подтверждено проверкой |
| 7 | Админка: проверить + управление обращениями + тестовый доступ | 🟡 почти готово — ответ клиенту сделан (PR #7, ждёт мёржа+проверки), Telegram-алерт уже был из Phase 3 (Phase 6) |
| 8 | Максимум бесплатного Cloudflare | ✅ готово (кроме R2, не нужен пока) (Phase 8) |
| 9 | План + todolist | ✅ готово — этот файл |

**Единственный внешний блокер: TG-1 (Telegram-токен). Всё остальное можно делать прямо сейчас.**

Решения, зафиксированные в начале (не пересматриваем без причины):
- **Почта**: Cloudflare Email Routing — `sales@nanosudo.com` остаётся на сайте, но реально форвардится на `roorsayan@gmail.com`. Текст на сайте не меняем.
- **Хостинг**: остаёмся на Vercel. Cloudflare подключаем поверх — DNS, Email Routing, Turnstile, Web Analytics.
- **Telegram**: бота создаёте вы сами — пошаговая инструкция ниже.
- **Admin-доступ**: вход останется magic-link (без пароля) — это безопаснее.

✅ **Уточнено по почте**: `roorsayan@gmail.com` — основной адрес (используется везде: Cloudflare, Supabase admin_members, Vercel). `roorrsayann@gmail.com` — дополнительный. Дальше по проекту как адрес по умолчанию использую основной.

---

## Phase 0 — доступы и предпосылки

**Итог живой проверки: почти всё уже было готово до старта работы. Реальный блокер остался только один — Telegram.**

- `[x]` **CF-1** Домен nanosudo.com уже на Cloudflare (NS: `david`/`fish`.ns.cloudflare.com, регистратор — тоже Cloudflare). Ничего переключать не нужно.
- `[x]` **CF-2 / MAIL-1** Email Routing уже включён и настроен: правило `sales@nanosudo.com → roorsayan@gmail.com`, статус **Active**, адрес-получатель **Verified** (создано ~5 месяцев назад). Проверил напрямую в Cloudflare Dashboard.
- `[x]` **ADM-1** В `admin_members` уже есть два администратора: `sayan@nanosudo.com` и `roorsayan@gmail.com` (оба с марта 2026). Whitelist делать не пришлось — вы уже там. Просто заходите на `/admin`, вводите `roorsayan@gmail.com`, получаете magic-link.
- `[x]` **MAIL-2** Проверил Vercel → Environment Variables (прод):
  - `RESEND_NOTIFICATION_EMAIL` (Production) = `sales@nanosudo.com` — корректно, письма уйдут туда и форварднутся вам через Email Routing.
  - `RESEND_API_KEY` (Production) — задан, похож на настоящий ключ. Помечен в Vercel бейджем **"Needs Attention"**, но это не ошибка конфигурации, а совет Vercel пометить переменную как *Sensitive* (шифрование), т.к. значение похоже на секрет и сейчас видно в открытом виде всем с доступом к проекту. `BREVO_API_KEY` — та же самая пометка, по той же причине.
  - Brevo для уведомлений по заявкам сейчас **не используется** — только для рассылки (newsletter). Реальный путь уведомлений: brief/support routes → **Resend** → `sales@nanosudo.com` → Cloudflare Email Routing → ваша почта.
  - `[ ]` **MAIL-3** (осталось): не проверено, реально ли Resend успешно отправляет — ключ выглядит валидным, но живого теста ещё не было. Нужно отправить тестовую заявку через `/brief` и убедиться, что письмо дошло. Если дойдёт — вся цепочка почты уже рабочая, и «нет реальных обращений» это, скорее всего, вопрос трафика/SEO (Phase 7), а не доставки писем.
  - `[ ]` **SEC-1** (новое, низкий приоритет): пометить `RESEND_API_KEY` и `BREVO_API_KEY` в Vercel как *Sensitive* по рекомендации самого Vercel.
- `[!]` **TG-1** Telegram-бот — единственный настоящий блокер. Вы создаёте сами по инструкции ниже, присылаете мне token + chat_id.
- 📌 Bonus-находка для Phase 8: в Cloudflare Turnstile уже есть один виджет — `Zakyat Verification` (от другого вашего проекта). Для nanosudo.com своего виджета нет — заведём его отдельно в рамках **CF-3** (Phase 8).

### Инструкция: Telegram-бот за 2 минуты
1. В Telegram откройте **@BotFather** → `/newbot` → задайте имя и username (например `nanosudo_leads_bot`)
2. BotFather пришлёт **token** вида `123456789:AAExxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx` — сохраните
3. Напишите вашему новому боту любое сообщение (например «привет»)
4. Откройте в браузере: `https://api.telegram.org/bot<TOKEN>/getUpdates` — в ответе найдите `"chat":{"id": ЧИСЛО, ...}` — это ваш `chat_id`
5. Пришлите мне token и chat_id (или сохраните их сами в Vercel env — я тогда просто задокументирую имена переменных)

---

## Phase 1 — Hero-секция ✅ готово
- `[x]` **HERO-1** Убрать фото и фамилию, перестроить композицию — сделано в `hero-section.tsx`: единая центрированная колонка, заголовок теперь про результат для клиента (использован уже готовый переведённый ключ `home.hero.title` с `<highlight>`, вместо «Привет, я Sayan»), фото и photo-card убраны. Вместо фото — под секцией добавлена `TechnologiesMarquee` (реальный стек технологий, бегущая строка) как сигнал доверия вместо личного фото. Соцсети (LinkedIn/Instagram/Telegram/GitHub) оставлены — это контактный сигнал, а не персональный брендинг, в одну строку под CTA.
- `[x]` **HERO-2** i18n не пришлось трогать — нужный ключ `home.hero.title` уже существовал и был переведён на ru/en/kk (остался неиспользуемый `mainTitle`/`photoPlaceholder`/`imageAlt` — не удалял, чтобы не плодить риски, это просто мёртвые ключи)
- `[x]` **HERO-3** Проверено вживую (`pnpm dev` + скриншот): рендерится корректно, ошибок в консоли нет. Убрали `<Image priority>` с большим фото — LCP-нагрузка ниже, чем раньше. Формальный Lighthouse-прогон не делал — пока полагаюсь на визуальную проверку.

## Phase 2 — визуальная согласованность фона по секциям ✅ готово
- `[x]` **BG-1** Аудит показал: общий фон на самом деле уже единый и глобальный — `BackgroundInfrastructure` (общая сетка `.grid-pattern`, шаг 60px) рендерится один раз в `layout.tsx` и растянут на весь сайт. Компоненты `components/background/*` и `hero-background.tsx` — мёртвый код, нигде не подключены, на верстку не влияют (не трогал, не наш случай, чтобы не плодить риск). Причина расхождений — не общий фон, а **один** самовольный `process-section.tsx`: он рисовал поверх глобальной сетки свою вторую сетку с ДРУГИМ шагом (48px против глобальных 60px → отсюда «линии не совпадают»), плюс уникальный тёмный оверлей и 4 разноцветных pastel-пятна (`#99b9ff`/`#78ffd1`/`#ffb3c2`/`#f0ffa6`), которых нет больше нигде на сайте — отсюда «где-то есть свечение, где-то нет».
- `[x]` **BG-2** Убрал у `process-section.tsx` весь самодельный фоновый слой (вторую сетку, оверлей, 4 пятна) — секция теперь показывает ту же самую глобальную сетку, что и остальные 5 секций. Единая система де-факто уже существовала (`BackgroundInfrastructure` + `glass-card` + hover-glow в акцентном цвете на карточках) — просто одна секция из неё выпадала.
- `[x]` **BG-3** Стыки секций выровнены: добавил `border-t border-border/60` в `final-cta-section.tsx` (единственная секция, где раньше не было разделителя) — теперь у всех 5 секций после Hero одинаковый ритм разделительных линий. Проверено вживую (`pnpm dev` + скриншоты по всей главной странице, тёмная тема) — сетка визуально совпадает от секции к секции, лишних пятен нет, ошибок в консоли нет.

## Phase 3 — Telegram-уведомления по заявкам ✅ готово
- `[x]` **TG-2** `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID` добавлены в `src/config/env.server.ts` (zod, optional — без них фича просто молчит, ничего не ломает)
- `[x]` **TG-3** `src/server/telegram/notify.ts` — `sendTelegramMessage()` через голый `fetch` (без новых зависимостей) + `buildTelegramMessage()`/`escapeTelegramHtml()` для единообразных сообщений и защиты от поломки Telegram HTML-разметки пользовательским текстом
- `[x]` **TG-4** Подключено во все 4 роута: `api/brief/route.ts`, `api/brief-new/route.ts`, `api/brief-simple/route.ts`, `api/support/route.ts` — отправка идёт параллельно с email через тот же `Promise.allSettled`, ошибка Telegram не роняет остальные уведомления
- `[x]` **TG-5** Формат сообщения: тип заявки/тикета, имя, контакт (email/телефон), ключевые детали (отрасль/стоимость/приоритет — по ситуации), прямая ссылка на запись в админке. Секретов и лишнего PII в логи не пишем — только `console.error` с текстом ошибки при неудачной отправке
- `[x]` **TG-6** Гейт через `E2E_TEST_MODE` есть во всех 4 роутах (в `brief-new` добавил новый локальный гейт, там его раньше не было даже для email)
- `[x]` **TG-7** Живая проверка на проде: создан тестовый тикет `TSK-20260818-LE6G` через `/api/support` (nanosudo.com) → сообщение реально пришло в Telegram (@nanosudo_leads_bot → chat_id 926884201). Бот, токен и chat_id настроены и подтверждены рабочими; переменные добавлены в Vercel Production, задеплоено через `git push` (пустой коммит для форс-редеплоя после добавления env).

Также по пути обновил `.env.example`: задокументировал `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID` и заодно закрыл пробел — `RESEND_*` переменные (уже давно используются в коде) там раньше вообще не были описаны.

📌 Тестовый тикет `TSK-20260818-LE6G` остался в `service_requests` — удалите сами через `/admin/service-requests`, когда удобно (не стал трогать вашу БД без явной просьбы).

## Phase 4 — кейсы/портфолио ✅ готово
- `[x]` **CASE-1** Удалил кейсы `kazpravda` и `amiray-kz` из `src/lib/portfolio-data.ts` (52 и 48 строк соответственно) + удалил их переводы (`cases.projects.kazpravda`/`amiray-kz`) из `messages/ru.json`, `en.json`, `kk.json` — по 23 строки на файл, чисто, без мусора
- `[x]` **CASE-2** Причина обрезки найдена и исправлена: контейнер под картинку на главной — почти квадратный (543×570px, ratio 0.95), а исходный скрин — широкий 2880×1800 (ratio 1.6). При центрированном `object-cover` обрезало ~40% по бокам, ровно посередине текста заголовка. Поставил `object-position: left top` (через inline `style`, т.к. Tailwind-класс `object-[0%_0%]`/`object-left-top` почему-то не собирался в CSS у Turbopack dev — возможно кэш) — теперь виден весь заголовок целиком, обрезается только правый край с мокапом карточки (уместный компромисс). Проверено вживую скриншотами.
- `[x]` **CASE-3** Заменил `public/cases/nativetravel.jpg` на `explore-ru.png` из `~/Projects/nativetravel/outputs_build/screenshots/` (карта + список объектов — понятнее показывает суть AI-маркетплейса, чем демо-страницы с плейсхолдер-контентом). Конвертировал в JPEG, 2880×1800, тот же формат, что и остальные кейсы.
- `[x]` **CASE-4** Удалил `public/cases/kazpravda.jpg` и `public/amiray-kz-portfolio.jpg` (`git rm`) — предварительно проверил, что больше нигде не упоминаются
- `[x]` **CASE-5** Sitemap (`src/app/sitemap.ts`) и `/cases` генерируются динамически из `getAllProjects()` — удалённые кейсы пропали автоматически, хардкод-упоминаний счётчиков не найдено

## Phase 5 — доставка обращений (email)
- `[x]` **MAIL-1 / CF-2** Cloudflare Email Routing включён и активен — см. Phase 0
- `[x]` **MAIL-2** прод env в Vercel сверен — см. Phase 0
- `[ ]` **MAIL-3** Сквозной тест: реальная заявка с брифа → письмо реально приходит на `roorsayan@gmail.com` — единственный оставшийся шаг в этой фазе

## Phase 6 — админка для обращений/тикетов 🟡 почти готово, ждёт вашей живой проверки

- `[x]` **SEC-2** (найдено при живой проверке) Логин-форма `/admin` слала magic link и **создавала аккаунт** для любого введённого email — реальная проверка "админ или нет" была только в RLS, не на входе. Из-за этого при попытке войти с `roorrsayann@gmail.com` (не тот адрес, что в `admin_members`) получилась пустая панель без единой ошибки — выглядело как баг с данными, а на деле — левый аккаунт без прав. Исправлено (`shouldCreateUser: false`) — PR #9, смёржен. Побочный эффект: в `auth.users` остался один лишний аккаунт `roorrsayann@gmail.com` без доступа ни к чему (не трогал, безвредно висит).
- `[x]` **ADM-2** Аудит `/admin/service-requests`: список с фильтрами (статус/приоритет/тип/поиск) — есть; деталка тикета — есть смена статуса по ITIL-переходам (new→acknowledged→in_progress→resolved→closed), SLA-таймеры с индикацией просрочки, внутренние заметки, заметки о решении, причина постановки на hold. Чего не хватало: (1) нельзя было ответить клиенту письмом прямо из панели, (2) не было ссылки этого на Telegram. `/admin/submissions` (брифы) — отдельный, более простой список статусов (new/in_progress/completed/archived), без ответа клиенту — не трогал, в исходном запросе речь шла именно про обращения за поддержкой (service_requests).
- `[x]` **ADM-3, часть 1** Ответ клиенту из админки — сделано. Админка была чистым SPA без своего API-слоя (пишет в Supabase напрямую из браузера), поэтому для email понадобился новый защищённый эндпоинт `POST /api/admin/service-requests/[id]/reply` (проверяет вызывающего по Supabase-токену + `admin_members`, шлёт письмо через Resend, логирует в `internal_notes` с таймстампом — без миграции схемы). На детальной странице тикета — карточка «Ответ клиенту». При первом ответе на «Новый» тикет статус авто-переключается на «Принят». PR #7, ждёт мёржа + вашей живой проверки (нужен вход в `/admin`).
- `[x]` **ADM-3, часть 2** Telegram-алерт на новый тикет — уже сделано в Phase 3 (TG-4/TG-7), отдельно делать не пришлось.
- `[x]` **ADM-1** whitelist для тестового входа — см. Phase 0 (уже был настроен)

## Phase 7 — SEO под рынок Казахстана

⚠️ **Важное решение по адресу** (пересмотрено после разговора с пользователем): точный домашний адрес/координаты в LocalBusiness-разметке и футере **оставлены как есть** по прямой просьбе — не трогать. GBP/2ГИС с публичным адресом — низкий приоритет, т.к. клиенты физически не приходят; для Google Business Profile при заведении использовать режим **Service Area Business** (адрес приватный только для верификации, публично не показывается).

### 📊 Реальные данные из Google Search Console (уже подключён, проверено вживую)
Ключевая находка: агрегированные метрики домена **включают поддомен `nta.nanosudo.com`** (это отдельный проект — NativeTravel, листинги театров/музеев Алматы) — он искажает картину. После фильтрации (показатели только для самого nanosudo.com, 3 месяца):
- **7 кликов, 134 показа, CTR 5.2%, средняя позиция 6.1** — сайт ранжируется НЕПЛОХО там, где есть контент под конкретный запрос. Проблема не «низкие позиции», а **малый охват** — мало страниц вообще получают показы (всего ~21 URL за 3 месяца).
- Топ-страница по показам — блог-пост `/ru/blog/kz-tax-2026-developer-advantages` (налоговые льготы разработчикам РК, 33+7=40 показов) — явно резонирует со спросом. **Вывод: контент-стратегия работает, когда есть релевантный контент — нужно больше таких постов**, это и есть главный рычаг, не техника.
- Проверил на дубли (URL с `/ru/` и без) — ложная тревога, редирект и canonical настроены верно, само переиндексируется.

### Статусы
- `[x]` **SEO-1** Аудит + LocalBusiness-разметка — см. PR #12 (адрес оставлен как есть, см. решение выше)
- `[x]` **SEO-1b** (новое) Google Search Console — уже был подключён и верифицирован (DNS TXT), проверил вживую, собрана реальная статистика (см. выше)
- `[x]` **SEO-1c** (новое) Яндекс.Вебмастер — не был подключён вообще. Завёл сайт, добавил TXT-запись верификации в Cloudflare DNS сам (`yandex-verification: 56796424addd258c`) — верификация в процессе, подтвердится автоматически в течение ~2 дней. После подтверждения нужно будет отправить sitemap (`https://nanosudo.com/sitemap.xml`) через раздел «Индексирование» в Вебмастере.
- `[x]` **SEO-2** Обновлено 19.08.2026: живой сбор из Google Trends (KZ, 12 мес.) + Яндекс.Вордстат (пользователь залогинился сам, регион Казахстан) + разведка выдачи Google по конкурентам. Реальные показы: «заказать сайт» 613/мес, «n8n» 643/мес (почти всё техническое), «kaspi api» 131/мес, «заказать сайт под ключ» 52/мес. Важные корректировки: «стоимость сайта» (352/мес) и «автоматизация бизнеса» (199/мес) оказались в основном мусором (МГУ-тюишн и бренд-запрос одной компании соответственно) — реальный релевантный объём в разы меньше. «интеграция 1с сайт», «веб-разработчик казахстан», «it аутсорс казахстан» — 0–1 показ, снял/понизил в приоритете. По просьбе пользователя проверил и казахский язык: «сайт жасату»/«бизнесті автоматтандыру» — 0 показов; «сайт жасау» (89) на 85% — сайты-приглашения на той, не бизнес; «жасанды интеллект» — 1719/мес, но почти весь объём образовательный/развлекательный, не B2B. Вывод: казахоязычного SEO под эту нишу сейчас не оправдано, коммерческий спрос в KZ идёт на русском. По просьбе пользователя также расширил охват тематических запросов за пределы старого черновика («сайт под ключ» отдельно от «заказать», «сайт для бизнеса», «лендинг», «продвижение сайта»): главная находка — **«продвижение сайта»/SEO-услуги — 2359 показов/мес, крупнейший реальный кластер спроса из всех проверенных**, в 4 раза больше «заказать сайт», и его не было в старом плане вообще. Проверил код — SEO у наносудо сейчас только техническая характеристика сборки, не отдельная услуга; добавил это как открытый вопрос (пункт 10 плана публикаций + решение №3), не как готовую рекомендацию. Полный разбор с исправленной таблицей приоритетов — в артефакте (см. ниже).
- `[ ]` **SEO-3** GBP (режим Service Area Business, без публичного адреса) — понижен в приоритете по вашему решению, делать не обязательно. 2ГИС — не делать, ценности для бизнеса без офиса нет.
- `[x]` **SEO-4** Контент-план пересмотрен 19.08.2026 с учётом реальной конкуренции — ниша ИИ-автоматизации оказалась плотнее, чем считалось (Shipmint.kz, n8n.kz, Serverspace.kz, SOWITA, AISol, ASPEX уже публикуют контент на те же темы); Kaspi Pay API гайды уже насыщены 5 конкурентами. Финализирован в тот же день в полноценный **годовой план**: 6 контент-кластеров → 16 тем → календарь на 12 месяцев (1 статья/мес база + 4 доп. темы при ускорении до 2/мес), плюс отдельный раздел GEO/AEO — видимость в ChatGPT/Perplexity/Gemini (Q&A-схема, llms.txt, кейсы с цифрами, внесайтовые упоминания — Clutch/GoodFirms/Habr/2ГИС, сравнительный контент, ежеквартальная бесплатная проверка через прямые вопросы к ИИ). Полный план — артефакт: https://claude.ai/code/artifact/9246efbb-6a05-49af-85b6-5040d842ed59. Решение о темпе/авторе публикаций и о запуске SEO-продвижения как услуги всё ещё за вами (см. раздел 10 артефакта).
- `[x]` **SEO-5** Техническое SEO — sitemap.ts/robots.ts в порядке, дублей URL нет (проверено), canonical корректный
- `[x]` **SEO-6** Замкнуто: Telegram (Phase 3) + Cloudflare Web Analytics + GSC/Яндекс.Вебмастер (Phase 7) — полная видимость воронки трафик→заявка уже есть

### Черновой список ключевых слов (нужна ваша сверка приоритетов)
**Коммерческие / общие:**
`заказать сайт казахстан` · `разработка сайтов казахстан` · `веб-разработчик казахстан` · `создание веб-приложений казахстан` · `стоимость разработки сайта казахстан`

**ИИ-автоматизация (ваша ниша, меньше конкуренции):**
`ии автоматизация казахстан` · `автоматизация бизнес процессов казахстан` · `n8n казахстан` · `make.com автоматизация казахстан` · `внедрение чат-бота казахстан` · `claude api интеграция`

**Интеграции (высокий commercial intent в KZ):**
`интеграция kaspi api` · `интеграция 1с сайт` · `crm интеграция казахстан`

**Локальные:**
`веб-разработка алматы` · `сайт под ключ алматы` · `it аутсорс казахстан`

### Черновой контент-план (8 тем, приоритет сверху вниз)
1. «Сколько стоит сайт в Казахстане в 2026: разбор по типам проектов» — закрывает самый частый вопрос, ведёт на /brief
2. «Kaspi API: как подключить приём платежей на сайте» — узкая ниша, мало конкурентов в KZ-контенте
3. «n8n vs Make.com: что выбрать для автоматизации бизнеса в Казахстане» — ловит нишевый ИИ-трафик
4. «Как ИИ-агент на Claude API экономит бизнесу часы ручной работы — реальный кейс»
5. «Интеграция сайта с 1С: пошаговое руководство» — техническая, но конвертит B2B
6. «Next.js vs WordPress: что выбрать для бизнес-сайта в 2026» — сравнительная, много поискового спроса
7. «Чек-лист: что спросить у веб-разработчика перед стартом проекта» — доверие + внутренние ссылки на /services
8. «Кейс: как автоматизация сэкономила [клиент] N часов в месяц» — привязать к реальному кейсу из портфолио, реальные цифры

Каждая статья: тайтл с ключом, один явный CTA на /brief или /contact, 800-1500 слов, минимум 2 внутренние ссылки на кейсы/услуги.

## Phase 8 — максимум бесплатного Cloudflare ✅ готово
- `[x]` **CF-3** Turnstile-виджет `nanosudo-forms` создан в Cloudflare (Managed mode), подключён в код на всех формах-заявках (`/brief` → brief-simple, `/support`) через переиспользуемый `TurnstileWidget`/`useTurnstile()` + серверную проверку `verifyTurnstileToken()`. Env-переменные (`NEXT_PUBLIC_TURNSTILE_SITE_KEY`, `TURNSTILE_SECRET_KEY`) добавлены в Vercel Production. Проверено вживую в dev: виджет рендерится, кнопка отправки корректно блокируется без прохождения проверки (тестировали на localhost — там ожидаемо не проходит, т.к. виджет привязан только к хосту nanosudo.com — это и подтверждает, что защита реально работает).
  - Заодно защитил и `/api/brief` (старая форма) и `/api/brief-new` (форма с калькулятором) — но по факту обе сейчас **не подключены** ни к одной странице (`BriefFormProvider` и `brief-page-client-wrapper.tsx` нигде не импортируются из `src/app`). Реально работает через `/brief` только **brief-simple** (3 шага). Защита на неиспользуемых роутах не мешает, но если эти формы возвращать в строй — они уже готовы.
- `[x]` **CF-4** Web Analytics — оказался уже включён и активно собирает данные (27 визитов/38 просмотров за 24ч на момент проверки, реальные Core Web Vitals) — ничего настраивать не пришлось, это zone-level фича Cloudflare для проксированных доменов.
- `[x]` **CF-2** Email Routing — см. Phase 0/5, уже активен
- `[ ]` **CF-6** Оценить перенос хранения PDF/брендбуков в Cloudflare R2 (10GB бесплатно) вместо Supabase Storage — только если упрёмся в лимиты Supabase
- `[ ]` **CF-7** Cache/Page Rules на статику поверх Vercel CDN
- `[ ]` **SEC-1** Пометить `RESEND_API_KEY` и `BREVO_API_KEY` в Vercel как *Sensitive* (см. Phase 0)

---

## Phase 9 — загрузка изображений в редакторе статей и кейсов ✅ готово

Контекст: рассматривали перенос админ-редактора статей с `egemenkz` (Tiptap/ProseMirror WYSIWYG) — сравнил стеки, дал технический ответ. Решили **не мигрировать формат хранения** (markdown, ~10k строк существующего блог-контента остаются совместимыми) — пользователю нужна была только ручная вставка изображений/скриншотов, без полной миграции на WYSIWYG.

- `[x]` **IMG-1** Bucket `blog-images` создан в Supabase Storage (public, 5MB лимит, png/jpeg/webp/gif) — через Storage REST API (DDL-доступа к базе через сервисный ключ нет, только через SQL Editor)
- `[x]` **IMG-2** RLS-политики на `storage.objects` для `blog-images` (read/insert/delete — только `admin_members`, по образцу существующего `brandbooks`) — миграция `supabase/migrations/20260819000000_create_blog_images_bucket.sql`, выполнена вручную в SQL Editor на проде, `Success. No rows returned`
- `[x]` **IMG-3** `src/lib/blog-image-upload.ts` — общая утилита загрузки (валидация типа/размера, сохранение в bucket, опциональная папка-префикс), `uploadBlogImage()` и `uploadCaseImage()` (папка `cases/`)
- `[x]` **IMG-4** Редактор статей (`blog-editor.tsx`): поле «Обложка» теперь с кнопкой загрузки файла (не только руками вставлять URL); в поле «Контент (Markdown)» — кнопка «Вставить изображение», вставляет `![](url)` в позицию курсора
- `[x]` **IMG-5** `/admin/projects` (кейсы): страница была голой заглушкой «в разработке» без единой функции — добавлен `CaseImageUploader` (виджет: выбор файла → загрузка в Storage → превью + копируемая публичная ссылка). Это НЕ полноценная CMS для кейсов — сами кейсы по-прежнему редактируются в `src/lib/portfolio-data.ts` через код, виджет только снимает шаг «куда закинуть файл». Пользователь явно выбрал этот минимальный вариант вместо полной CMS.
- PR #14, смёржен в `main`.

📌 Побочная находка: bucket `brandbooks` (используется только мёртвым роутом `/api/brief`) в проде на самом деле не существует — не чинил, т.к. затрагивает только неиспользуемый код.

## Phase 10 — вложения в обращениях поддержки (скриншоты + ссылки)
- `[x]` **SUP-1** `service_requests` получил колонки `attachment_urls text[]` и `links text[]` — миграция `supabase/migrations/20260821000000_support_attachments.sql`, выполнена на проде 21.08.2026 (колонки+комментарии+RLS через SQL Editor: `Success. No rows returned`; bucket создан отдельно через Storage REST API, т.к. `storage.create_bucket()` упал с ошибкой несовпадения сигнатуры функции — тот же нюанс, что и в Phase 9).
- `[x]` **SUP-2** Новый bucket `support-attachments` (public, 5MB, png/jpeg/webp/gif) с RLS **anon insert** — сознательно другая модель, чем `blog-images`: загружает анонимный посетитель сайта на форме `/support`, а не залогиненный админ. Admin-only select/delete для управления из дашборда.
- `[x]` **SUP-3** `src/lib/support-attachment-upload.ts` — загрузка скриншота напрямую из браузера в Storage (не через API-роут base64-в-JSON, как `/api/brief` делает для брендбука) — так надёжнее для нескольких файлов сразу: JSON-body с base64 4×5MB рисковал упереться в лимит тела запроса на Vercel serverless.
- `[x]` **SUP-4** Форма `/support` (`_client.tsx`): загрузка до 4 скриншотов (превью + удаление) и до 5 ссылок (динамический список полей) — обе секции только пишут уже готовые URL/строки в react-hook-form через `setValue`, без блокировки основной Turnstile-защиты формы.
- `[x]` **SUP-5** `/api/support` сохраняет оба поля; админ-письмо и Telegram-уведомление показывают вложения (кликабельные ссылки на скриншоты + список ссылок, счётчик в Telegram).
- `[x]` **SUP-6** Админка (`service-request-detail.tsx`): новая карточка «Вложения от клиента» — превью скриншотов (клик → полный размер в новой вкладке) и список ссылок.
- i18n: ключи `support.form.attachments*`/`links*`/`addLinkButton` добавлены в ru/en/kk.
- `pnpm typecheck` / `pnpm lint` / `pnpm test` — чисто.

✅ Применено на проде 21.08.2026 — PR #15 смёржен в `main`, миграция выполнена, bucket подтверждён `GET /storage/v1/bucket` → `support-attachments` присутствует, колонки подтверждены через REST (`attachment_urls`/`links` читаются, `null` на существующих тикетах). Фича полностью в проде.

## Phase 11 — реальный фавикон вместо дефолтного Next.js
- `[x]` **FAV-1** `src/app/favicon.ico` оказался нетронутым файлом-заглушкой Next.js по умолчанию (чёрный круг с белым треугольником) — Next.js автоматически отдаёт этот файл по спец-конвенции на `/favicon.ico` **независимо** от `<link rel="icon">`/`metadata.icons` в `layout.tsx`, которые уже правильно указывали на настоящий логотип. Именно `/favicon.ico` чаще всего запрашивают напрямую браузеры и генераторы превью ссылок (Telegram и т.п.) — отсюда «стандартный логотип в ссылках» вместо настоящего.
- `[x]` **FAV-2** Перегенерировал `src/app/favicon.ico` из `public/Nanosudo_logo_favicon.png` (реальный логотип) через ImageMagick — мультиразмерный `.ico` (16/32/48/64px). Остальные иконки (`favicon-for-app/apple-icon.png`, `icon1.png`) уже были брендированы правильно, трогать не пришлось.
- Проверено: `curl localhost:3000/favicon.ico` отдаёт новый файл, извлечённые кадры 16×16/32×32 визуально проверены — логотип читаем.

## Phase 12 — обновление скриншотов кейсов (живые проекты)
- `[x]` **SHOT-1** Проверил доступность всех 6 кейсов с `url` в `portfolio-data.ts` — двумя независимыми путями (curl из шелла и реальная навигация в браузере): **invite.kz, rals.kz, okiyo.kz** живые (HTTP 200); **egemen (64.177.67.133:3000), kazgazeta (45.77.41.77), zakyat.kz** сейчас недоступны (таймаут на обоих путях — сервер/домен не отвечает, не проблема окружения). `nativetravel` — в кейсе вообще нет поля `url` (проект в активной разработке), не входит в объём задачи.
- `[x]` **SHOT-2** Обновил скриншоты трёх живых сайтов (`public/cases/invite.jpg`, `rals.jpg`, `okiyo.jpg`) — реальные, актуальные на 22.08.2026 снимки, а не заглушки. На okiyo.kz предварительно закрыл cookie-баннер вариантом «Только необходимые».
- Технический нюанс: первая попытка (resize+cover-crop до 2880×1800, как у старых файлов) обрезала логотипы/навигацию по краям — окружение отдаёт скриншот фиксированного размера 1512×788 независимо от `resize_window`. Решение: сохранил нативную пропорцию скриншота, апскейл 2x (Lanczos) → 3024×1576, без обрезки контента. Сайт не хардкодит 2880×1800 нигде в коде (только `object-fit`/`object-position` в CSS), так что другое соотношение сторон не ломает вёрстку — проверено вживую на `/cases`.
- Не трогал: egemen/kazgazeta/zakyat-kz (недоступны сейчас — обновить, когда снова будут в сети) и nativetravel (нет публичной ссылки).

### Обновление: реальные ссылки нашлись — SHOT-1 частично устарел
Пользователь дал настоящие продакшн-домены вместо демо-IP: **egemen.kz**, **kazgazeta.kz**, **kazpravda.kz**, **nta.nanosudo.com/ru/explore**, **rals.kz/catalog**. Проверил каждый вживую в браузере:
- `[x]` **SHOT-3** `egemen` — url обновлён `http://64.177.67.133:3000` → `https://egemen.kz`, скриншот обновлён.
- `[x]` **SHOT-4** `kazgazeta` — url обновлён `http://45.77.41.77` → `https://kazgazeta.kz`, скриншот обновлён (подтвердил по факту: логотип «Qazaq Gazetteri» + «Ұлан»/«Ақ желкен» на сайте совпадают с описанием кейса).
- `[x]` **SHOT-5** `nativetravel` — добавлено поле `url: "https://nta.nanosudo.com/ru/explore"` (раньше отсутствовало), скриншот обновлён на реальную карту с листингами.
- `[x]` **SHOT-6** `rals` — url остался `https://rals.kz` (главная), но скриншот теперь снят со страницы `/catalog` — нагляднее показывает e-commerce каталог, чем главная.
- `[x]` **SHOT-7** **Восстановлен кейс `kazpravda`** — был удалён в Phase 4 (CASE-1, коммит `d0316dc`) с демо-IP `http://65.20.89.81:3000`. Пользователь дал реальный домен `kazpravda.kz` («Казахстанская правда» — самостоятельное издание, не то же самое, что egemen/kazgazeta). Восстановил **дословно оригинальный текст** кейса из git-истории (без выдумывания нового) в `portfolio-data.ts` + `messages/{ru,en,kk}.json`, только `url` заменён на реальный домен и добавлен свежий скриншот `public/cases/kazpravda.jpg`.
- Валидация: `pnpm typecheck`/`lint`/`build` — чисто. Живая проверка на `/cases` и `/cases/kazpravda` — рендерится корректно.
- 📌 Дев-сервер (`next dev`, Turbopack) при этом стабильно показывал one-off "Recoverable Error" (hydration mismatch ровно на границе, где вставлен kazpravda) — перепроверил **напрямую в собранном проде** (`pnpm build` + инспекция `.next/static/chunks/*.js` на диске): оба кейса (kazpravda И kazgazeta) присутствуют в одном и том же собранном клиентском чанке с полностью корректными данными. Это подтверждённый баг/артефакт Turbopack dev-режима (React сам себя чинит, помечено как "Recoverable"), не реальная проблема кода — фиксировать нечего.

### Исправление качества скриншотов (по фидбэку пользователя)
Пользователь справедливо указал: скриншоты вышли **плохого качества и неподходящего размера**. Причина — предыдущий подход апскейлил нативный снимок (1512×788, потолок разрешения в этом окружении — `resize_window` его не меняет) в 2 раза через Lanczos, чтобы попасть в старую конвенцию 2880×1800. Апскейл интерполяцией никогда не добавляет реальной детализации — только смазывает: отсюда и «плохое качество».
- `[x]` **QUAL-1** Пересмотрел вообще без апскейла. Нашёл, что реальный размер контейнера под эти картинки в коде — `aspect-video` (16:9) и на `/cases` (список), и на `/cases/[id]` (деталка) — а не 2880×1800 (16:10) и не мой предыдущий вариант (≈1.92:1). Раньше это нигде не проверял, просто скопировал старую конвенцию.
- `[x]` **QUAL-2** Пересобрал все 7 файлов из уже сохранённых сырых скриншотов (папка временных файлов ещё не была очищена ОС — повторно сайты не дёргал): чистая обрезка по центру до 16:9 **на нативном разрешении**, без единого пикселя интерполяции — `magick -gravity center -extent`. Результат: 1401×788 (большинство) / 1424×801 (egemen) / 1422×800 (nativetravel), резкий текст, логотипы не обрезаны ни на одном из 7.
- `[x]` **QUAL-3** Проверил: контейнер списка кейсов ограничен `max-w-2xl` (672px CSS) — то есть ~1400px нативной ширины с запасом хватает даже под 2x retina без всякого апскейла.
- Живая проверка на `/cases/okiyo` (детальная страница, тот самый `aspect-video`-контейнер) — картинка резкая, хорошо скомпонована, ничего важного не обрезано.

## Единственный содержательный блокер, который реально остаётся
Открытых внешних блокеров нет. Активная работа — SEO/контент-план (Phase 7, годовой план — см. память `nanosudo-seo-content-plan`).
