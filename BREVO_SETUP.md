# Brevo Email Configuration

## ✅ Настройка завершена

### Добавленные переменные в `.env.local`:

```bash
BREVO_API_KEY="xkeysib-xxxxx...xxxxx"  # Your Brevo API key from dashboard
NEXT_PUBLIC_BREVO_SENDER_EMAIL="noreply@nanosudo.com"
BREVO_NOTIFICATION_EMAIL="roorrsayann@gmail.com"
```

> **Note**: API ключ уже настроен в локальном `.env.local` файле

## 📧 Как работают уведомления

### При отправке брифа через форму:

1. **Администратору** (`roorrsayann@gmail.com`):
   - Тема: "Новый бриф: [Название компании]"
   - Содержание: Полная информация о заявке
   - Вложение: PDF отчет с деталями брифа
   - Reply-To: Email клиента (для быстрого ответа)

2. **Клиенту** (Email указанный в форме):
   - Тема: "Получили ваш бриф — спасибо!"
   - Содержание: Подтверждение получения заявки
   - Вложение: PDF отчет с его запросом

## 🧪 Тестирование

### Локально:
```bash
pnpm dev
# Перейти на http://localhost:3000/brief
# Заполнить форму и отправить
```

### Production:
```bash
# Убедиться что переменные добавлены в Vercel:
# Settings → Environment Variables
BREVO_API_KEY=...
NEXT_PUBLIC_BREVO_SENDER_EMAIL=...
BREVO_NOTIFICATION_EMAIL=...
```

## 🔍 Проверка отправки

### API endpoint:
- **URL**: `POST /api/brief`
- **Location**: `src/app/api/brief/route.ts`

### Логика отправки:
1. Валидация данных (Zod schema)
2. Сохранение в Supabase (`submissions` table)
3. Загрузка brandbook файла (если есть) в Supabase Storage
4. Генерация PDF отчета
5. **Отправка 2 email** через Brevo API:
   - Админу: с полной информацией
   - Клиенту: с подтверждением

### Проверка статуса в Brevo Dashboard:
- URL: https://app.brevo.com
- Statistics → Transactional → Email logs
- Смотреть статус отправки (Sent/Delivered/Opened)

## ⚠️ Важные заметки

1. **E2E_TEST_MODE**: В тестовом режиме email не отправляются
2. **Promise.allSettled**: Email отправляются параллельно, ошибка одного не блокирует другой
3. **Fallback**: Если `BREVO_NOTIFICATION_EMAIL` не указан, используется `contactEmail` из формы

## 📝 Структура данных

### Brief submission включает:
- Информация о клиенте (название, индустрия, география)
- Целевая аудитория и каналы
- KPI и метрики
- Контактные данные
- Brandbook (опционально)

### Генерируется:
- UUID для submission
- PDF отчет
- Email уведомления

## 🔧 Troubleshooting

### Email не приходят:
1. Проверить API key в `.env.local`
2. Проверить статус в Brevo Dashboard
3. Проверить spam/junk folder
4. Проверить логи: `pnpm dev` покажет ошибки API

### Ошибка "BREVO_API_KEY is not configured":
- Убедиться что переменная в `.env.local`
- Перезапустить dev сервер

### PDF не прикрепляется:
- Проверить генерацию: `src/server/pdf/brief-report.ts`
- Проверить base64 encoding
