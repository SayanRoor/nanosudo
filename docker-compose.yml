version: '3'

services:
  web:
    container_name: nanosudo-web
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    env_file:
      - .env.local
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
